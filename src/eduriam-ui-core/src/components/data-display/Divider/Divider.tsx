import MuiDivider from "@mui/material/Divider";

/**
 * A horizontal divider for visually separating content.
 *
 * Wraps Material UI's Divider with no additional props.
 */
export const Divider: React.FC = () => {
  return <MuiDivider />;
};

export default Divider;
